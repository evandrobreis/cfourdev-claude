# Onde o trabalho parou. Curto e descartavel: o que merece sobreviver a semana
# pertence ao modelo, a uma decisao ou ao project-context.yaml.
version: 2
updated: AAAA-MM-DD

# Em que assunto o trabalho estava.
focus: |
  ...

# A ultima operacao: o comando que rodou e o que ele fez. E o que permite
# retomar sem reconstruir a conversa.
last_operation: |
  cfour element add loja-api --parent loja --shape api  ->  criou a caixa; check ok

# O proximo passo, acionavel. "Continuar a modelagem" nao e proximo passo.
next_step: |
  ...

# O que voce propos e o arquiteto ainda nao confirmou. NAO escreva no modelo o
# que estiver aqui.
pending:
  - ...

# Arquivos tocados nesta sessao, para a proxima saber onde olhar.
touched_files:
  - ...

# Contagens de `cfour check --modelagem <id> --inventory --json` quando a sessao
# fechou. Se ao retomar elas nao baterem, alguem editou o modelo fora do plugin.
model_fingerprint:
  elements: 0
  relations: 0
  diagrams: 0
  flows: 0
  notes: 0
  errors: 0
  warnings: 0
